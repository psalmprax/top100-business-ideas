---
phase: 06-global-security-performance-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - R6.3
user_setup: []

must_haves:
  truths:
    - "All E2E tests pass (100% success rate)"
    - "Test suite runs without environment issues"
    - "No flaky tests (3/3 consecutive passes)"
  artifacts:
    - path: "e2e-test-results.json"
      provides: "Test execution report with pass/fail counts"
    - path: "playwright-report/index.html"
      provides: "HTML test report"
  key_links:
    - from: "client/src/test/*.spec.ts"
      to: "pnpm test:e2e"
      via: "Playwright"
---

<objective>
Complete R6.3: E2E Test Suite Pass (100% success)

Purpose: Run E2E tests, fix failures, achieve 100% pass rate.

Output: All E2E tests passing, full test coverage verified.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**Test Scripts (from package.json):**

- `pnpm test:e2e` → `playwright test`

**Existing Test Files:**

- client/src/test/e2e.spec.ts
- client/src/test/e2e-functional.spec.ts
- client/src/test/e2e.test.ts
- client/src/test/sentinel_hard_integrity.spec.ts
- client/src/test/alpha-products.spec.ts
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Run E2E Test Suite</name>
  <files>client/src/test/</files>
  <read_first>
- client/src/test/e2e.spec.ts
- client/src/test/e2e-functional.spec.ts
- package.json (scripts)
  </read_first>
  <action>
Run the Playwright E2E test suite:

1. Run `pnpm test:e2e` and capture full output
2. Parse which tests pass and which fail
3. For each failure, record:
   - Test name
   - Error message
   - Root cause (missing feature, environment, test bug, auth)
4. Save results to `e2e-test-results.json` in format:

   ```json
   {
     "total": N,
     "passed": N,
     "failed": N,
     "flaky": N,
     "failures": [{"name": "...", "error": "...", "cause": "..."}]
   }
   ```

5. Also generate HTML report using `--html` flag if available
   </action>
   <verify>
   <automated>pnpm test:e2e 2>&1 | tail -50</automated>
   </verify>
   <done>

- Test suite executed
- All pass/fail results captured
- Root causes identified
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix Test Environment Issues</name>
  <files>client/src/test/, playwright.config.ts</files>
  <read_first>
- playwright.config.ts (if exists)
- Look for test configuration
  </read_first>
  <action>
Fix identified environment issues:

For each environment issue found in Task 1:

1. **Missing API endpoints:**
   - Check if the backend is running
   - Update test to handle offline/missing gracefully OR skip with explanation

2. **Authentication failures:**
   - Check for valid auth tokens
   - Implement proper login flow in tests OR add test user setup
   - Use test credentials from environment

3. **Missing test data:**
   - Create test fixtures/seeds
   - Add beforeEach hooks to set up data

4. **Timing issues:**
   - Add explicit waits where needed
   - Increase timeouts for slow operations

5. **Configuration issues:**
   - Fix playwright.config.ts baseURL, timeouts, etc.

Prioritize issues that block multiple tests first.
</action>
<verify>
<automated>ls -la playwright.config.ts 2>/dev/null || echo "No config found"</automated>
</verify>
<done>

- Environment issues identified
- Configuration fixes applied
- Tests can connect to required services
  </done>
  </task>

<task type="auto">
  <name>Task 3: Fix Failing Tests</name>
  <files>client/src/test/</files>
  <read_first>
- The specific test files that are failing (from Task 1)
  </read_first>
  <action>
Fix individual failing tests:

For each test failure identified in Task 1 (that isn't environment-related):

1. **Missing features:**
   - If the feature doesn't exist, mark test as skipped with reason
   - Or create a minimal implementation

2. **Test bugs:**
   - Fix assertions to match actual behavior
   - Fix selectors that changed
   - Fix incorrect test logic

3. **Flaky tests:**
   - Add proper waits forasync operations
   - Remove timing dependencies
   - Add retry logic if appropriate

4. **After fixes:**
   - Run tests again to verify
   - Run 3 times to confirm no flakiness

Focus on achieving 100% pass rate. If a test cannot pass due to missing functionality outside phase scope, mark it as skipped with clear documentation.
</action>
<verify>
<automated>pnpm test:e2e 2>&1 | grep -E "passed|failed|skipped" | tail -5</automated>
</verify>
<done>

- All fixable tests fixed
- Skipped tests documented with reasons
- 100% pass rate achieved (or close + documented)
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                  |
| ------------------------- | ---------------------------- |
| Test Runner → Application | Automated input crosses here |
| Test Data → Database      | Test data persists here      |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                           |
| --------- | ---------------------- | ---------------- | ----------- | ----------------------------------------- |
| T-06-11   | Tampering              | Test Data        | mitigate    | Tests clean up after themselves           |
| T-06-12   | Repudiation            | Test Results     | mitigate    | Archive test reports with timestamps      |
| T-06-13   | Information Disclosure | Test Credentials | mitigate    | Use test-only credentials, not production |
| T-06-14   | Denial of Service      | Test Run         | accept      | Tests are isolated, limited impact        |
| T-06-15   | Elevation of Privilege | Test Admin       | accept      | Tests run with limited permissions        |

</threat_model>

<verification>
- [ ] E2E tests executed
- [ ] Environment issues fixed
- [ ] Failing tests fixed
- [ ] 100% pass rate achieved
- [ ] Threat model complete
</verification>

<success_criteria>

- [ ] All three tasks completed
- [ ] Test results captured
- [ ] Environment issues resolved
- [ ] Failing tests fixed
- [ ] 100% pass rate (or documented skips)
- [ ] SECURITY: All 5 threats addressed

</success_criteria>

<output>
After completion, create `.planning/phases/06-global-security-performance-audit/06-03-SUMMARY.md`
</output>
