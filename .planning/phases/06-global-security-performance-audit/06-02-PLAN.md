---
phase: 06-global-security-performance-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - R6.2
user_setup: []

must_haves:
  truths:
    - "Cancellable biometric verification succeeds > 99% of time"
    - "Panic word detected within 2 seconds of utterance"
    - "Emergency response triggers correctly when panic word detected"
  artifacts:
    - path: "biometrics-verification-report.md"
      provides: "Security verification results including pass/fail rates"
    - path: "packages/livenesslink-sdk/src/panic-word.ts"
      provides: "Panic word detection module"
  key_links:
    - from: "client/src/components/auth/*"
      to: "@livenesslink/sdk"
      via: "React hook integration"
---

<objective>
Complete R6.2: Final verify Cancellable Biometrics & Panic Word flow

Purpose: Verify the LivenessLink SDK integration and implement panic word detection for emergency scenarios.

Output: Biometrics verified working, panic word detection implemented.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/livenesslink-sdk/src/index.ts

**LivenessLink SDK exists at:** `packages/livenesslink-sdk/src/index.ts`

**SDK Features:**

- Liveness verification (detects live person vs deepfake)
- Biometric verification (face, voice, iris)
- Fraud detection (deepfake, mask, replay attacks)
- Webhook configuration
- Analytics

**Missing:** Panic word detection - needs implementation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Biometric SDK Integration</name>
  <files>packages/livenesslink-sdk/src/index.ts</files>
  <read_first>
- packages/livenesslink-sdk/src/index.ts
- packages/livenesslink-sdk/package.json
  </read_first>
  <action>
Verify the LivenessLink SDK is properly integrated:

1. Check the SDK exports and types are correct
2. Verify the React hook (`useLivenessLink`) works with the app
3. Check if there's a configured API key or if it needs environment setup
4. Document the integration points in the app

Test by checking if @livenesslink/sdk is imported anywhere in the client code.
</action>
<verify>
<automated>grep -r "@livenesslink/sdk" --include="_.ts" --include="_.tsx" . 2>/dev/null | head -20</automated>
</verify>
<done>

- SDK integration verified
- Integration points documented
- Any missing configuration noted
  </done>
  </task>

<task type="auto">
  <name>Task 2: Implement Panic Word Detection</name>
  <files>packages/livenesslink-sdk/src/panic-word.ts</files>
  <read_first>
- packages/livenesslink-sdk/src/index.ts
- Look for any existing audio/speech processing in the codebase
  </read_first>
  <action>
Implement panic word detection module:

1. Create `packages/livenesslink-sdk/src/panic-word.ts` with:
   - `PanicWordDetector` class
   - Methods: `startListening()`, `stopListening()`, `onPanicDetected(callback)`
   - Default panic words: "help", "emergency", "help me", "call police"
   - Configurable panic words via constructor

2. Use Web Speech API (SpeechRecognition) for browser-based detection:
   - Real-time speech-to-text
   - Keyword matching against panic words
   - Low threshold for detection (sensitive mode)

3. Export the module from index.ts

4. Include fallback: if Web Speech API unavailable, log warning and disable
   </action>
   <verify>
   <automated>ls -la packages/livenesslink-sdk/src/panic-word.ts 2>/dev/null && echo "File exists"</automated>
   </verify>
   <done>

- Panic word detection module created
- Exported from main SDK
- Emergency callback system works
  </done>
  </task>

<task type="auto">
  <name>Task 3: Create Emergency Response Integration</name>
  <files>client/src/</files>
  <read_first>
- Look for any existing emergency/security components
- Look for navigation/routing code
  </read_first>
  <action>
Create emergency response integration:

1. Create a hook `usePanicMode()` that:
   - Imports and uses PanicWordDetector
   - Triggers emergency state when panic word detected
   - Provides `activatePanicMode()`, `deactivatePanicMode()`, `isPanicMode: boolean`

2. When panic mode activated:
   - Immediately end any active sessions
   - Clear sensitive data from memory
   - Navigate to safe screen
   - Log the event (for audit)

3. Document the integration in a comment at the top of the file

4. Export from the SDK for client use
   </action>
   <verify>
   <automated>grep -r "panicMode\|usePanicMode" --include="_.ts" --include="_.tsx" . 2>/dev/null | head -10</automated>
   </verify>
   <done>

- Emergency response hook created
- Panic mode state management works
- Sessions cleared on panic activation
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                |
| --------------------------- | -------------------------- |
| Browser → Microphone        | Audio input crosses here   |
| Speech API → Application    | Untrusted audio processing |
| Panic Detection → Emergency | Critical action boundary   |

## STRIDE Threat Register

| Threat ID | Category               | Component       | Disposition | Mitigation Plan                                            |
| --------- | ---------------------- | --------------- | ----------- | ---------------------------------------------------------- |
| T-06-06   | Tampering              | Panic Detection | mitigate    | Verify microphone access is authorized before activation   |
| T-06-07   | Repudiation            | Panic Events    | mitigate    | Log all panic activations with timestamps and session IDs  |
| T-06-08   | Information Disclosure | Biometric Data  | mitigate    | Never store raw biometric templates, only encrypted hashes |
| T-06-09   | Denial of Service      | Speech API      | accept      | Browser-dependent, fallback gracefully                     |
| T-06-10   | Elevation of Privilege | Panic Mode      | mitigate    | Require authentication before deactivating panic mode      |

</threat_model>

<verification>
- [ ] SDK integration verified
- [ ] Panic word detection implemented
- [ ] Emergency response hook created
- [ ] Threat model complete
</verification>

<success_criteria>

- [ ] All three tasks completed
- [ ] Biometric SDK verified working
- [ ] Panic word detection implemented
- [ ] Emergency response integrated
- [ ] SECURITY: All 5 threats addressed

</success_criteria>

<output>
After completion, create `.planning/phases/06-global-security-performance-audit/06-02-SUMMARY.md`
</output>
