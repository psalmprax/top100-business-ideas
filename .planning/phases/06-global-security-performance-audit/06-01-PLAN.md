---
phase: 06-global-security-performance-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - R6.1
  - R6.2
  - R6.3
user_setup: []

must_haves:
  truths:
    - "All subagent API calls respond within 500ms (P95 latency)"
    - "Cancellable biometric verification succeeds > 99% of time"
    - "Panic word detected within 2 seconds of utterance"
    - "E2E test suite achieves 100% pass rate"
  artifacts:
    - path: "benchmark-results.json"
      provides: "Performance latency measurements"
    - path: "biometrics-verification-report.md"
      provides: "Security verification results"
    - path: "e2e-test-results.html"
      provides: "Test execution report"
  key_links:
    - from: "client/src/test/*.spec.ts"
      to: "pnpm test:e2e"
      via: "Playwright test runner"
    - from: "API Gateway"
      to: "Performance metrics"
      via: "Timing middleware"
---

<objective>
Complete Phase 6: Global Security & Performance Audit

Purpose: Validate subagent performance, verify security features (biometrics & panic word), and achieve 100% E2E test pass rate.

Output: 3 plans covering performance, security verification, and E2E testing.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-global-security-performance-audit/06-RESEARCH.md

**Phase Requirements:**

- R6.1: Validate subagent performance (latencies < 500ms)
- R6.2: Final verify Cancellable Biometrics & Panic Word flow
- R6.3: E2E Test Suite Pass (100% success)

**Tech Stack:**

- Frontend: React 19 (Vite, TypeScript)
- API Gateway: Go (Gin)
- Intelligence Hub: Python (FastAPI)
- Tests: Playwright + Vitest
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Run E2E Test Suite and Identify Failures</name>
  <files>client/src/test/*.spec.ts</files>
  <read_first>
- client/src/test/e2e.spec.ts
- client/src/test/login.spec.ts
- client/package.json (scripts)
  </read_first>
  <action>
Run the Playwright E2E test suite using `pnpm test:e2e` and capture all test results. Parse the output to identify:
1. Which tests pass
2. Which tests fail
3. The error messages for each failure
4. Whether failures are due to: missing features, environment issues, test bugs, or authentication problems

Document findings in a structured format for the next task to fix.
</action>
<verify>
<automated>pnpm test:e2e --reporter=json > e2e-results.json 2>&1 || true; cat e2e-results.json | head -100</automated>
</verify>
<done>

- Test suite has been executed
- All pass/fail results captured
- Root causes identified for each failure
  </done>
  </task>

<task type="auto">
  <name>Task 2: Performance Latency Validation</name>
  <files>server/go/cmd/api/main.go</files>
  <read_first>
- server/go/cmd/api/main.go (API server entry point)
- server/python/app/api/agents.py (subagent endpoints)
- Any existing timing/middleware code
  </read_first>
  <action>
Create a performance benchmark script that:
1. Makes API calls to each subagent endpoint
2. Measures response time for each call
3. Calculates P50, P95, P99 latencies
4. Tests with both single requests and concurrent load (10, 50, 100 concurrent)

Run benchmarks against the API Gateway and Intelligence Hub. Verify all subagent calls respond within 500ms (P95).

If latency exceeds 500ms, identify the bottleneck and document it.
</action>
<verify>
<automated>go run ./benchmark/main.go --output=benchmark.json 2>&1 || echo "Benchmark script needs implementation"</automated>
</verify>
<done>

- Performance benchmarks executed
- P95 latency measured for all subagent endpoints
- Results show < 500ms or bottleneck identified
  </done>
  </task>

<task type="auto">
  <name>Task 3: Cancellable Biometrics & Panic Word Verification</name>
  <files>packages/livenesslink-sdk</files>
  <read_first>
- packages/livenesslink-sdk/src/index.ts (if exists)
- client/src/components/ (any biometric-related components)
- Look for "panic", "liveness", "biometric" in codebase
  </read_first>
  <action>
Research the current implementation of:
1. Cancellable biometrics — verify enrollment and verification flows exist
2. Panic word detection — check if audio processing detects trigger words
3. Emergency response — confirm panic triggers appropriate action

Search for: panic word, liveness, biometric, cancelable, emergency in the codebase.

Document what exists, what's missing, and what needs verification/testing.
</action>
<verify>
<automated>grep -r "panic\|liveness\|biometric\|cancelable" --include="_.ts" --include="_.tsx" --include="\*.py" . 2>/dev/null | head -50</automated>
</verify>
<done>

- Current biometric implementation documented
- Panic word detection mechanism found or documented as missing
- Verification approach determined
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                       |
| ------------------------------ | --------------------------------- |
| client → API Gateway           | Untrusted user input crosses here |
| API Gateway → Intelligence Hub | Internal service communication    |
| Biometric sensor → Application | Hardware-level trust boundary     |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                    |
| --------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------------ |
| T-06-01   | Tampering              | E2E Tests        | mitigate    | Tests must verify data integrity between client and server         |
| T-06-02   | Repudiation            | Performance Logs | mitigate    | Log all latency measurements with timestamps                       |
| T-06-03   | Information Disclosure | Biometric Data   | mitigate    | Ensure cancellable biometrics don't expose raw biometric templates |
| T-06-04   | Denial of Service      | API Endpoints    | mitigate    | Performance tests verify service handles load without failure      |
| T-06-05   | Elevation of Privilege | Panic Word       | mitigate    | Verify panic word cannot be spoofed to gain elevated access        |

</threat_model>

<verification>
- [ ] E2E tests executed, results captured
- [ ] Performance benchmarks run, P95 < 500ms verified
- [ ] Biometric security features documented
- [ ] Threat model included
</verification>

<success_criteria>

- [ ] All three tasks completed
- [ ] E2E test results captured
- [ ] Performance latency measured
- [ ] Biometric implementation documented
- [ ] SECURITY: Threat model complete with all 5 threats addressed
      </success_criteria>

<output>
After completion, create `.planning/phases/06-global-security-performance-audit/06-01-SUMMARY.md`
</output>
