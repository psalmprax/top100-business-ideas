# Core web framework and async runtime
fastapi>=0.115.0
uvicorn[standard]>=0.31.1

# Data validation and settings
pydantic>=2.12.5
pydantic-settings>=2.4.0

# Authentication and security
python-jose[cryptography]==3.3.0
PyJWT>=2.12.0
passlib[bcrypt]==1.7.4
cryptography>=43.0.1
Authlib==1.3.0

# Database and ORM
sqlmodel==0.0.14
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.1

# File handling and multipart
python-multipart>=0.0.9
aiofiles==23.2.1

# HTTP client
httpx>=0.28.1

# Environment and utilities
python-dotenv>=1.2.1
itsdangerous==2.1.2
psutil==5.9.8

# Business logic dependencies
stripe==11.4.0

# AI/ML Core (Lazy-loaded for faster container builds)
numpy==1.26.3
pillow==10.2.0
opencv-python-headless==4.9.0.80

# Heavy ML dependencies - moved to optional extras for lazy loading
# torch==2.2.0
# torchvision>=0.17.0
# transformers==4.36.2
# librosa==0.10.0

# Agent frameworks (optional - loaded on demand)
crewai==0.83.0

# External AI services (optional)
langchain-community>=0.4.1
langchain-core>=1.3.0
langchain>=1.2.15

# External AI services
openai>=1.0.0
duckduckgo-search==6.3.3
ddgs>=9.0.0

# Reporting (optional)
# reportlab==4.0.0  # Commented out due to cairo dependencies

# External Git dependency - pinned to specific release for stability
hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git@v2026.4.8#egg=hermes-agent

# Local AgentOps SDK
-e ../../packages/agentops-sdk-python

# Missing dependencies for tools and scraping
beautifulsoup4>=4.12.3
playwright>=1.42.0
